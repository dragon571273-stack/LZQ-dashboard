# REITs Workspace — UI UX Pro Max 设计系统

## 产品与检索依据

专业机构投资者使用的中国公募REITs投研工作台。现有技术栈为原生HTML/CSS/JavaScript与ECharts，不迁移到未经检测的框架。

已安装并执行 ui-ux-pro-max 的 design-system 检索（institutional finance analytics dashboard；复核 financial trading dashboard），密度8、差异5、动效2。有效匹配为 Data-Dense Dashboard、蓝色金融数据配色、等宽数字与无障碍规则。自动输出的 Enterprise Gateway 营销首页结构与本项目任务不符，未保存或应用其销售CTA、Hero视频及客户Logo模块；工作台布局采用人工设计的侧栏导航与任务分区。

补充查询：导航层级（ux）；category comparison bars（chart）匹配横向条形对比，数值与正负符号直接标注。OHLC图建议因现有首页只有价格指数序列，不适用。

## 视觉规范

- 默认浅色：背景 #F4F6FA，内容 #FFFFFF，主文 #17243C，次文 #5C6980，线 #E5EAF2。
- 主色 #3159CE；深色侧栏 #15243D；选中态 #2B466D；品牌保留现有狮子标志。
- 金色只用于研究标签和原品牌，不用作全页背景。涨跌沿用中国市场红涨绿跌，均配符号与数值。
- 字体：中文 PingFang SC / Microsoft YaHei；标题 Avenir Next + 中文回退；数字 SFMono-Regular / Consolas，tabular-nums。该选择为中文与离线性能适配，不依赖Google字体连接。
- 字号 12/13/14/16/20/28/32；正文16px，表格13px，注释12px。长段落限制阅读宽度。
- 圆角：控件6px，面板12px。单层轻阴影，仅用于浮层。
- 间距：4/8/12/16/24/32。动效仅状态与交互120–180ms，无持续装饰动画。

## 页面架构

桌面：216px固定侧栏 + 64px工作区顶栏 + 页面标题 + 二级页签 + 数据内容。
市场：4个核心指标 → 等权价格指数 / 板块对比 → 热力图 / 市场观察。
研究：沿用所有分析模块，以统一标题、页签、过滤器、图表和表格容器展示。
配置：六个任务页签，默认总览含条件性判断与研究入口；详细估值、现金流、板块、压力测试和来源按需显示，避免巨型长报告。
机构间市场：指标行、筛选、项目表统一风格，保留排序与查询。

375/768px：侧栏收为可关闭抽屉；菜单具有焦点管理与Escape退出；页签局部横向滚动，所有数据表独立滚动。1024px侧栏收窄；1440px展示标准双列分析布局。

## 数据边界

原始行情、历史序列、财务模型和研究文件不改写。新增首页图表为原序列的只读窗口归一化；必须展示实际序列日期。分派、估值数据不足时不虚构评级。
