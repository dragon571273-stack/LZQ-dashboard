# 招投标更新诊断及验证

## 已复现的问题

GitHub Actions 34916732415 显示 ctbpsp 连续两次返回零条，旧站 HTTP 请求两次返回 405，但 news 更新时间仍推进。原 cron 仅工作日执行。原查询起始日期硬编码为 1994-06-24，仅取两页，历史合并以标题前20/25字去重。

正常浏览器访问指定新站时停留在加载/访问验证阶段，因此未把它报告为成功。HTTPS 备用列表成功返回公告；真实数据每页20条，旧HTML显示代码仍使用15条，57条只需3页。第4页重复第3页，故改为按总记录数而非旧页面样式常量终止。

## 最终设计

独立工作流每天北京时间09:20/18:20运行；ctbpsp为主源、官方HTTPS列表为备用、深圳环水官网最新公告为局部补充。保留90天历史；源状态、成功时间、公告日期分离；重复页和访问限制不会被当成完整成功。

原始行情与财务模型没有改动。数据写入专用 tenders.json，新闻正文仍由原流程维护。已移除每日行情流程中重复的旧招投标调用和无用的Scrapling安装。

## 验证

10项Python回归测试、24项Node测试通过；语法与diff检查通过。本地真实运行：ctbpsp未成功、官方备用57条/3页、深圳环水首页无REITs命中；筛选90天与历史合并后64条，最新公告2026-09-14。主源失败状态如实公开，不保证受访问限制的新站在每次运行中可用。

样本测试固定来自官方公开列表的两条公告，检查发布日期而非开标日。前端独立数据加载与完整标题去重有回归覆盖。
